# dependencies
node_modules/

# build output
dist/
**/target/

# logs
*.log

# env / secrets
.env
.env.*
!.env.example

# test coverage
coverage/

# os
.DS_Store
Thumbs.db

# Host crate is a library — apps own dep resolution.
crates/kriya/Cargo.lock

# Internal planning docs — kept local, not published (per planner, 2026-06-15).
docs/DECISIONS.md
docs/strategy/
# Reclassified local-only 2026-06-26: it carries implementation detail (signing-key/module/attestation
# specifics) that shouldn't sit on a public surface (console-public-restraint).
docs/SERVICE-ARCHITECTURE.md
# Reclassified local-only 2026-06-27: the "three frontiers" essay's premise (desktop has no agent
# tooling standard) is outdated — desktop has MCP-stdio too. The public positioning is now the
# governance / control-plane thesis (in the README); this essay is kept local for history only.
docs/THREE-FRONTIERS.md

# Patent drafts — MUST stay local. Public disclosure before filing destroys novelty
# (absolute novelty in IN/EU) and starts the US grace clock. Never commit/push.
docs/patents/

# Demo recording plans (local-only)
examples/actual-budget-bolt-on/DEMO.md
examples/actual-budget-bolt-on/REAL-DEMO.md
examples/actual-budget-bolt-on/demo.cast

# Local MCP host config — machine-specific paths (+ secrets for actual-budget). Commit .mcp.json.example.
examples/actual-budget-bolt-on/.mcp.json
examples/replicad-bolt-on/.mcp.json

# CAD geometry exports (output, not source)
*.stl

# local clones of Kriya integration targets (analysis only, not tracked)
.integration-targets/

# Python bytecode (doc-tooling scripts)
__pycache__/

# Local Claude Code config (machine-specific settings)
.claude/

# Internal / strategic materials — kept local, NOT for the public repo (planner, 2026-06-24).
brand/
pitch/
docs/whitepaper/
docs/sessions/
docs/research/
