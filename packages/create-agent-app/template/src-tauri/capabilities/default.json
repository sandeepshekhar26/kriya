{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows the app webview to use core events and invoke app commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
