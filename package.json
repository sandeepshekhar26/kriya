{
  "name": "agent-native-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "The framework where AI agents are native citizens of desktop apps.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  }
}
